# Example Command

This is an example slash command for Claude Code.

You can create custom commands by adding markdown files to this directory.
Each command file should describe what you want Claude to do when the command is invoked.

## Usage

Type `/example` in the Claude Code CLI to run this command.
